#include "main.h"

/**
 * _abs - function that computes the absolute of an integer
 * @i: integer to be compute
 * Return: the absolute value
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
